version: '3'
services:
  gcloud:
    build: .
    environment:
      TF_VAR_project: tftestyo
    volumes:
      - .:/app
    networks:
      - gcloud-test

networks:
  gcloud-test:
    driver: "bridge"
